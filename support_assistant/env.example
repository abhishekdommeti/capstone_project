# Left unset or set to 1: fully offline deterministic mock mode (graded baseline).
MOCK_LLM=1

# Only needed for the optional, ungraded MOCK_LLM=0 extension:
# MOCK_LLM=0
# GROQ_API_KEY=your-groq-free-tier-key
